import { FilterSubcategoryPipe } from './filter-subcategory.pipe';

describe('FilterSubcategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSubcategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
